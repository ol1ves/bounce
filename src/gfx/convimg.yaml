palettes:
  - name: global_palette
    images: automatic

converts:
  - name: sprites
    palette: global_palette
    images:
      - dvd_logo.png

outputs:
  - type: c
    include-file: gfx.h
    palettes:
      - global_palette
    converts:
      - sprites

